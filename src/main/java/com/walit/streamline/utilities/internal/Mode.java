package com.walit.streamline.utilities.internal;

/**
 * Simple enum to represent the different modes that the app can run in.
 * @author wellatleastitried
 */
public enum Mode {
    CACHE_MANAGEMENT,
    TERMINAL, // DEFAULT BEHAVIOR
    HEADLESS,
    TESTING
}
